# Aggregate requirements for back-compat with `pip install -r requirements.txt`
# advertised in the README quickstart. Splits live in:
#   requirements-base.txt  — proxy, demos, v2 domain tests, integration
#   requirements-llm.txt   — adds Anthropic (optional cloud backend)
#   requirements-ml.txt    — adds torch / transformers / scikit / numpy / datasets
#   requirements-dev.txt   — base + llm + ml (CI-style "everything")
-r requirements-dev.txt
